# 会话刷新流程

一次性场景：客户端发现会话临近过期时重新读取会话状态。

## 前置条件

- 客户端已持有会话令牌
- 模型：Session

## 步骤

### 步骤1 读取会话

- 调用 `GET /session`
- 配置键：session.ttlMs

### 步骤2 判断是否需要重新登录

- 若 expiresAt 临近，则提示客户端重新登录
- 模型：Session

## 后置条件

- 会话状态已确认
- 过期会话返回 E_SESSION_EXPIRED
