# 运行时配置

运行时配置契约：键 → 类型 → 默认值 → 必填 → 敏感。

## 配置项

| 键 | 类型 | 默认值 | 必填 | 敏感 | 说明 |
|---|---|---|---|---|---|
| session.ttlMs | integer | 3600000 | 否 | 否 | 会话有效期（毫秒） |
| session.maxActive | integer | 1000 | 否 | 否 | 最大并发会话数 |
