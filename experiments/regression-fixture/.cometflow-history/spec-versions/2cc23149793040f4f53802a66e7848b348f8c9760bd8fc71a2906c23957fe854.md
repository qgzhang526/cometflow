---
capability: report
module: src/report
---

# report capability

## REPORT-001 summary

汇总能力。

## Acceptance

- A1：summary 返回字符串
  - check: node --experimental-strip-types --input-type=module -e "const m = await import('./src/report/index.ts'); process.exit(typeof m.summary() === 'string' ? 0 : 1)"
