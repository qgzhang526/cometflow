---
capability: auth
module: src/auth
---

# auth capability

## POST /login

登录接口。

## Acceptance

- A1：合法请求返回 200
  - check: node --experimental-strip-types --input-type=module -e "const m = await import('./src/auth/index.ts'); process.exit(m.login() === true ? 0 : 1)"
