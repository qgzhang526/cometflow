---
capability: core
module: src/core
---

# core capability

## CORE-001 add

核心加法能力。

## Acceptance

- A1：add(1,2) 返回 3
  - check: node --experimental-strip-types --input-type=module -e "const m = await import('./src/core/index.ts'); process.exit(m.add(1, 2) === 3 ? 0 : 1)"
