# persistence capability

本地持久化最高分与统计。

## FR-PERSIST-001 高分存取

对局结束时记录最高分，重开后读取并展示。

## 验收

- A301：对局结束写入最高分文件
- A302：重开后读取历史最高分并展示
- A303：多次对局只保留最高分

## FR-PERSIST-002 健壮性

文件缺失或损坏时不崩溃，可安全重建。

## 验收

- A310：缺失文件首次运行时自动初始化
- A311：损坏文件被安全忽略并重建
- A312：存储路径遵循跨平台约定（不使用硬编码绝对路径）
